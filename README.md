# WordPress Playground

A Dockerized WordPress development environment for local development.

## Getting Started

Check out the `01-dev-environment` branch for the full development setup:

```bash
git checkout 01-dev-environment
make setup
make build
make start
make db-seed
```

Then access the site at https://playground.finder.dev
